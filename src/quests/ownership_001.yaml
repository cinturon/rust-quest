id: ownership_001
title: "Move a String"
zone: "Forest of Ownership"
instructions: |
  Create a `String` named `message` with content `"quest accepted"`.
  Move it into a second variable `banner` (do not clone).
  Print `banner` and leave `message` unused after the move.
xp: 15
victory_message: "The string moved once and never looked back. Quest accepted."
starter: |
  fn main() {
      // your code here
  }
verify:
  - kind: Compile
    expected: "App Compiles"
  - kind: Output
    expected: "quest accepted"